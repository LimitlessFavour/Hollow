name: hollow
description: Hollow assessment app
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  authentication_repository:
    path: packages/authentication_repository
  bloc: ^8.0.2
  device_preview: ^1.0.0
  dotted_border: ^2.0.0+2
  flutter:
    sdk: flutter
  flutter_bloc: ^8.0.1
  flutter_localizations:
    sdk: flutter
  form_inputs:
    path: packages/form_inputs
  freezed_annotation: ^1.1.0
  go_router: ^3.0.7
  hollow_design_system:
    path: packages/hollow_design_system
  hydrated_bloc: ^8.0.1
  intl: ^0.17.0
  json_annotation: ^4.4.0 
  overlay_support:
    path: packages/overlay_support
  path_provider: ^2.0.8
  shared:
    path: packages/shared 

dev_dependencies:
  bloc_test: ^9.0.2
  build_runner: 
  flutter_test:
    sdk: flutter
  freezed: ^1.1.0
  json_serializable: ^6.1.3
  mocktail: ^0.2.0
  very_good_analysis: ^2.4.0

flutter:
  uses-material-design: true
  generate: true

  assets:
    - assets/
    - assets/config/
    - assets/images/