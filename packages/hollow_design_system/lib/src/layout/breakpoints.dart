/// Defines the breakpoints for Hollow's UI.
abstract class AppBreakpoints {
  /// Max width for a small layout - watches etc.
  static const double small = 375;

  /// Max width for a medium layout - mobile phone, tabs
  static const double medium = 480;

  /// Max width for a large layout - desktop, web 
  static const double large = 1024;

}

/// Defines the breakpoints for Hollow's UI.
abstract class AppHeightBreakpoints {
  /// Max width for a small layout - watches etc.
  static const double small = 670;

  /// Max width for a medium layout - mobile phone, tabs
  static const double medium = 480;

  /// Max width for a large layout - desktop, web 
  static const double large = 1024;

}
